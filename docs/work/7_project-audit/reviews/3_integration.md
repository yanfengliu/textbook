# Review 3: integration

## Target

The `textbook` repository at `5e9c981`: its gates and their headers (`tools/`, `test/`), `docs/learning/defect-register.md` (22 entries, 43 defects), `docs/learning/lessons.md`, `docs/learning/gate-proofs.md`, the review record under `docs/work/`, the study code (`src/components/mastery.js`, `tools/progress.js`), `index.html`, `README.md` and `.github/workflows/`. Also read: the live front page, and the process state of every branch and worktree at a 10:03 snapshot on 2026-09-23. The question: are we blind to anything, ranked by consequence to the reader.

The tree can be recovered at `5e9c981`. The commit the report calls unpushed, `2376226`, was on `origin/fix-ch05` by the time this record was written. The rest of the 10:03 snapshot was not pinned and cannot be recovered exactly.

## Reviewers and coverage

`blind-spots`, a read-only worker the coordinator dispatched; the handoff does not record its model. Lens: the owner's fourth question, "Are we blind to anything?", from the side of what no gate, review or record sees. Access: the repository, every checkout on this machine and the live front page; its working files are in the session scratchpad's `blind-spots` folder. It tags claims READ, MEASURED or INFERRED.

Missing coverage, as the report states it: its check of the 143 figure-task items is text-level only; and the register it counts leaves out the reviewers' accuracy errors, so its count understates that class.

## Reports

### blind-spots

Ranked by consequence to the reader: 1. Nothing checks the biology is true — no gate reads meaning [READ]; every independent read found must-fix errors: ch01 6 in 3,718 words, ch02–03 8 in 19,199 words [READ]; the ch02–03 read was text-only and the item banks didn't exist then [READ]; chapters 4–5 went live unreviewed: 25,313 of 48,230 prose words (52%), 16 figures, 228 items, with 5e9c981's message saying the review "is owed" [MEASURED]; no review of any items.js bank is on record before the 09-23 chapter-5 review [READ]; 32 of 41 biology figures never checked against their captions [READ]; unpushed commit 2376226 on fix-ch05 fixes two false "an earlier chapter left this unexplained" sentences and an unhedged 40 °C claim the 2026-09-11 review had cut from ch02, all three still live in chapter 5 [MEASURED]. Cheap fix: a model review per chapter before publishing, tied to the page's digest and checked by npm run check. 2. The study record can claim knowledge the reader doesn't have, and the loop has never run — 233/233 MCQ items correct-first [MEASURED]; Today labels options A, B, C… and answers on A or 1 (mastery.js:799, :1094) [MEASURED]; npm run sitting answers "the first option offered", so it has only ever recorded right answers; fix in flight on choice-order [MEASURED]; progress/ 0 files, 0 events; docs/study/rounds/ only README; npm run review says "no progress recorded yet" [MEASURED]; tools/progress.js refers to an "export button" no page has — exportJSON() is called only by tests [MEASURED]; scheduler.test.js cannot tell whether its constants suit a human reader [READ]. 3. 11 published figures never contrast-measured: legible's DEFERRED holds ch04's eight (13 pairs under AA on 2026-09-16) and 通鑑's three (35 pairs) [MEASURED]. 4. The site misstates its own contents: the live front page says "Chapter 1… is ready; thirty-one more are outlined" (index.html:112, README.md:5) [MEASURED]; check-content's next-chapter rule reads only the end-of-chapter card [READ]. 5. No gate stands between main and readers: pages.yml deploys every push [MEASURED]; ci.yml's 30 minutes vs a ~2,322 s local chain [INFERRED]; no build stamp and nothing checks the site after deploy [READ]. 6. Everything past the first frame: sweep3d detects an absent frame, not a wrong one; drive asserts what a figure reports, not what it draws [READ]; the only viewport under 390 px is one 320 px shape [READ]; nothing throttles CPU or network [MEASURED]; 12 of 44 figure modules have no aria-live region [MEASURED]; none of the 143 figure-task items expects a field its figure never names [MEASURED, text-level]. sitting's header says "Tab and Enter alone" but its code calls focus() and never presses Tab [MEASURED].

Who found the defects (22 register entries, 43 defects, READ): owner 10 (device layout 6, content truth 2, i18n/fonts 1, gate wiring 1); reader-proxy model 4 (reading 3, i18n 1); model reviewer 1 entry (13 errors, content truth); gates 13 (device layout 6, timing 3, i18n/fonts 3, geometry 1); workers 15 (device layout 3, contrast 3, timing 1, data 1, gate wiring 3, typography 4). No gate found any content-truth or reading defect first; three gate finds were first dismissed as flaky; the register leaves out the reviewers' accuracy errors, so it understates that class.

Proofs owed / lessons ungated [MEASURED]: lessons.md has one queued lesson (the year-marking check); no gate-proofs.md entry names the 多音字 checks in lexicon.test, metabolism-table.test, theme.test, publish.test, the crash-dialog wiring check, or drive's ownership third branch. Still ungated per the register: hover firing, a card that won't dismiss, a card moving the text, glossary order, a chapter's claim about its own passage selection; the devices hang is unexplained.

Process state (snapshot 10:03): work only in a worktree: shot-repro (21 files), sort-layout (components.css +53/−15), fix-ch05 (2376226 + items.js), choice-order (3 files uncommitted after its commit), docs/work/6_mark-quality/plan.md in wt-provenance. Pushed but not on main: ch06-07 (1), items-ch06 (3), items-ch07 (3), ch08-dna (4). Nothing unique: wt-figure and wt-gate, fix-ch04. The devlog is not newest-first and has nothing after 2026-09-19. registry.json lists 0–4 while folders 5 and 6 exist; .git/work-docs-state.json holds only 0. Nothing since 2026-09-17 has a work folder; plan 2's live status is dated 2026-09-16; the coordinator's current status is not kept in the repo.

## Findings and disposition

The dispositions are the coordinator's, read off its synthesis in `../plan.md` (Outcome, parts 1–5 and 4(a)–(g), cited here as "synthesis 4(a)" and so on); the critic's checks are round 5. "Not taken up" means the synthesis does not mention the finding; it is not a rejection. The recorder added no judgement of its own.

| ID | Finding | Disposition and reason | Repair or follow-up |
|---|---|---|---|
| F28 | Nothing checks the biology is true. No gate reads meaning; every independent read found must-fix errors (ch01: 6 in 3,718 words; ch02–03: 8 in 19,199); chapters 4–5 — 25,313 of 48,230 prose words (52%), 16 figures, 228 items — went live unreviewed; no review of an `items.js` bank is on record before the 09-23 chapter-5 review; 32 of 41 biology figures were never checked against their captions; three errors fixed by `2376226` on `fix-ch05` are still live in chapter 5. Proposed: a model review per chapter before publishing, tied to the page's digest and checked by `npm run check`. | Accepted as the worst blind spot; synthesis 4(a). The coordinator's advice (synthesis 5) is an accuracy review of each chapter against the matching OpenStax Biology 2e section before it ships; it does not mention the digest-bound check proposed here. | Not started. The chapter 4–5 review is still owed (F7), and `2376226` is not on main. |
| F5 | The study record can claim knowledge the reader does not have: 233/233 multiple-choice items put the correct option first; Today labels options A, B, C… and takes answers on A or 1 (`mastery.js:799`, `:1094`); `npm run sitting` answers "the first option offered", so it has only ever recorded right answers. | As F5 in round 0; synthesis 4(b), which adds that the gate has only ever recorded right answers. | The fix is on `choice-order`, not merged. |
| F4 | The loop has never run: `progress/` holds 0 files and 0 events; `docs/study/rounds/` holds only its README; `npm run review` says "no progress recorded yet". | As F4 in round 0. | As F4. |
| F29 | No page has the "export button" that `tools/progress.js` refers to; `exportJSON()` is called only by tests. | Accepted; confirmed by the critic (round 5, check 5); synthesis 2 and 4(e). | The coordinator's advice, synthesis 5: an export button on Today. Not started. |
| F30 | `scheduler.test.js` cannot tell whether its constants suit a human reader. | Not taken up. | Open. |
| F27 | 11 published figures were never contrast-measured: legible's DEFERRED holds chapter 4's eight (13 pairs under AA on 2026-09-16) and 通鑑's three (35 pairs). | As F27 in round 2; synthesis 4(f). | None in this round. |
| F6 | The live front page says "Chapter 1… is ready; thirty-one more are outlined" (`index.html:112`, `README.md:5`), and check-content's next-chapter rule reads only the end-of-chapter card. | As F6 in round 0; synthesis 4(f). The bound on check-content's rule is not taken up. | None in this round. |
| F26 | No gate stands between main and readers: `pages.yml` deploys every push; `ci.yml`'s 30 minutes cannot hold a ~2,322 s local chain (INFERRED); there is no build stamp, and nothing checks the site after a deploy. | As F26 in round 2; synthesis 4(c). The build stamp and the check after deploy are not taken up. | As F25. |
| F31 | Everything past the first frame is unwatched: sweep3d detects an absent frame, not a wrong one; drive asserts what a figure reports, not what it draws; the only viewport under 390 px is one 320 px shape; nothing throttles CPU or network; 12 of 44 figure modules have no aria-live region; the `sitting` gate's header says "Tab and Enter alone" while its code calls `focus()` and never presses Tab. | Accepted in part: synthesis 4(d) takes what a figure reports against what it draws, and 4(e) the throttled phone, measured in round 4 (F36). The rest is not taken up. | Open. |
| F32 | Who found the 43 recorded defects: the owner 10, a reader-proxy model 4, a model reviewer 1 entry (13 errors), gates 13, workers 15. No gate found any content-truth or reading defect first; three gate finds were first dismissed as flaky; the register leaves out the reviewers' accuracy errors. | Recorded. The critic cites "workers found 15 of 43" (round 5, alternative (a)); the synthesis does not otherwise take it up. | None. |
| F33 | Proofs owed and lessons ungated: `lessons.md` holds one queued lesson (the year-marking check); no `gate-proofs.md` entry names the 多音字 checks in `lexicon.test`, `metabolism-table.test`, `theme.test` or `publish.test`, the crash-dialog wiring check, or drive's ownership third branch; hover firing, a card that won't dismiss, a card moving the text, glossary order and a chapter's claim about its own passage selection are still ungated; the devices hang is unexplained. | Not taken up. | Open. |
| F9 | Work only in a worktree (`shot-repro`, `sort-layout`, `fix-ch05`, `choice-order`, and `docs/work/6_mark-quality/plan.md` in `wt-provenance`), and work pushed but not on main (`ch06-07`, `items-ch06`, `items-ch07`, `ch08-dna`). | As F9 in round 0; synthesis 4(g). | Landing belongs to the owning session. |
| F34 | The allocation record is behind: `registry.json` lists 0–4 while folders 5 and 6 exist, and `.git/work-docs-state.json` holds only 0. | Not in the synthesis. The coordinator acted on it by telling this round to add the missing entries. | Partly repaired by this round: `registry.json` now lists 0–7 (`../plan.md`, Approach, "Allocation"). The witness file is untouched, and migrating the older units to the schema remains a round of its own. |
| F35 | The process record has stopped: the devlog is not newest-first and has nothing after 2026-09-19; nothing since 2026-09-17 has a work folder; plan 2's live status is dated 2026-09-16; the coordinator's current status is not kept in the repo. | Accepted; synthesis 4(g). | This folder is a work folder for this round. The devlog and the coordinator's live status are not repaired here. |

The rest of the report — its ranking, the per-class breakdown of who found which defects, and the text-level result that none of the 143 figure-task items expects a field its figure never names — is recorded as reported and carries no finding ID.

## Verification

The worker's checks were reads and counts over the repository and the checkouts, each tagged in the report; its working files are in the scratchpad's `blind-spots` folder, not in Git. The critic re-checked F5, F4 and F29 (round 5, checks 1 and 5). The recorder checked only that `2376226` is now on `origin/fix-ch05`.

## Round outcome

Accepted as the backbone of synthesis 4, with F28 as its worst blind spot, 4(a). Open and material: F28 (nothing checks accuracy, and the chapter 4–5 review is owed), F5 (answer order, fix unmerged), F4 and F29 (the study loop cannot run) and F26 (nothing stands between main and readers). F34 is partly repaired by this round's registry entries; nothing else was repaired, because the round was read-only.
