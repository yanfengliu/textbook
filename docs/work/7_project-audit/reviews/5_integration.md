# Review 5: integration

## Target

The approach behind the book as it stood at `5e9c981`: the goal, as the design documents and the owner's own requests state it; the way of working that rounds 0–3 describe; and the claims in those four handoffs, which the critic re-checked against the repository at `5e9c981`, the main checkout's reflog, the usage-limit messages in the session transcripts, and the GitHub Actions record. The question: is there a much better approach.

The repository and the Actions record can be recovered; the reflog and the transcripts are local to this machine and are not kept by this record.

## Reviewers and coverage

`approach-critic`, a read-only worker the coordinator dispatched; the handoff does not record its model. Lens: the owner's fifth question, "Is there a much better approach?", and an independent check of rounds 0–3 before the synthesis relied on them. Access: the repository, the reflog, the transcripts and `gh`; it ran its checks inline with node, grep and gh, and kept no files in the session scratchpad.

Missing coverage: its tokens-per-chapter and weeks-remaining figures are INFERRED from rounds 0–3; its checks cover claims from rounds 0–3, and it records no check of round 4.

## Reports

### approach-critic

Recommendation: budget each chapter at about 40M weighted tokens, half of today's ~70M, and pay for it with cuts no reader sees — (1) harness: compact near 250k tokens instead of ~970k, start a fresh coordinator each round, cut AGENTS.md to its rules and move the long Gates section into a doc agents look up, never leave a big-context agent idle past the 5-minute cache; (2) a figure cap: at most four bespoke interactive figures per chapter, each justified by what handling it teaches that prose can't, authored static illustrations elsewhere. Spend about 5% of the saving on what the reader lacks: an accuracy review of each chapter against the matching OpenStax Biology 2e section before it ships, and a study loop that can run (merge choice-order; an export button on Today so a phone's record reaches progress:import); after that, the study rounds' findings pick revision work, not open-ended polish. Gives up: about half the bespoke figures and figure tasks per chapter (tasks are 27% of items), full gate-chain runs on ordinary commits, and the recipe's "six to nine figures, no static diagram" rules — agents wrote those rules and the owner's own request names "illustrations", but overruling them is the owner's decision. Within a week: chapters 6–7 on main with 8 or fewer figures between them, a review note and green CI, for ≤80M weighted; average context per call under ~150k (from 315k); the weekly meter lasting at least 11 working hours (from ~7.8); progress/ holding at least 20 of the owner's own answers and docs/study/rounds/ its first note.

The goal (READ): a web book that "reads like a beautifully set book and behaves like a laboratory" — biology first, 32 chapters in six units at Campbell/OpenStax level, every figure authored, animated, simulated or 3D, no build step; the owner's standing bar "If anything is less than supreme quality then don't even bother"; a study system the owner asked for in the first person ("keep track of what I learned well and what I struggle to master… have the agent review my progress in rounds") with one default learner, a local record, and rounds only on the owner's command. INFERRED: the intended reader is the owner, and the public site is a by-product.

The bottleneck: the weekly usage allowance. Weekly-limit messages start two silences of 114 h and 134 h; the textbook's transcripts have records in only 40 of the 351 clock-hours since the first one (MEASURED). A chapter costs about 70M at the margin — eight figures at ~5.2M (227.5M across 44 figure modules) plus prose, items and overhead; the textbook's share of the account is roughly 190–280M a week (INFERRED). The current way of working isn't aimed at it (INFERRED): nothing counts tokens per chapter; 66% of tokens go on re-reading context; the most expensive agent (gate-speed, 21.8M) made gates faster while gate time isn't scarce; AGENTS.md doubled in two weeks and loads into every agent; each chapter makes every later full gate run longer — extrapolated to 32 chapters a full run takes about 2.8 h, and the rule is that it passes before every code commit.

Alternatives (tokens per chapter; weeks for the remaining 27 at ~235M a week; INFERRED): baseline ~70M, 8–12 weeks; (f) leaner context 15–25% cheaper, ~55M, ~6.5 weeks, losing nothing; (b) figure cap ~41M → ~25M in figures, ~50M alone, ~40M combined with (f), about 4.5 weeks — the biggest lever; shared templates alone won't get there (chapter 5's eight bench-built figures still average 805 lines, READ); (a) ad hoc probes are ~27% of tokens by step but workers found 15 of 43 recorded defects — get this saving through (e) rather than by decree; (e) a ~3-minute core per commit, the full chain at chapter landing, a CI that fits and blocks the deploy, plus the accuracy review — ~5% of tokens, its value is not publishing pages that fail CI or say false things; (c) OpenStax: the design names it as the level to write at and never rejects it; the no-raster rule excludes its figures; as a prose base it saves under 10% and costs the book's voice; as the reviewer's reference, at ~2–4M a chapter, it is the cheapest fix for the biggest gap (reviewed chapters had a must-fix error every 620–2,400 words, READ); (d) owner-driven: not possible yet (no phone export; every review-bank answer A) — about 1–3M fixes both; (g) standing records ~0.5% to write, their cost is being loaded into every agent, covered by (f). Also: all 32,254 model-tagged records name Opus 5, Opus 5.5 or Fable 5.1 — a smaller model for gate-running, waiting and landing helps only if the plan meters it cheaper; park the Chinese edition; a larger plan lifts the constraint directly (the owner's call).

Handoff checks (inline node, grep, gh): 1. correct-option-first is true for the 233 review-bank questions Today serves (mastery.js:799–805, no shuffle; MEASURED); "every multiple-choice answer on main is A" overstates — the 25 in-chapter questions put the answer at B (18) or C (7). 2. CI: 46 runs, 3 green, 43 red since 09-11 (MEASURED); "all 43 failed on the chapter-3 negative rect" is wrong — 0482dde shows ~3,000 negative-size errors but 6d854cf failed the content check; "39 of the last 40" holds. 3. Lockouts: the shape holds (limit messages at 09-11 07:49Z and 09-17 13:05Z), but "coverage complete" and "nothing ran inside them" are wrong — the main checkout's reflog has 19 commits between 10:11 and 20:19 PDT on 09-17 after that day's limit hit, and no Claude or Codex transcript on this machine covers them (MEASURED); the 599.7M total is a floor and the lockout share nearer 71%. 4. No figure since 09-17: true (newest is atp3d, 09-17 06:04), but about 5 of those 6 days were lockout, so it is a stall of about one working day. 5. No study round ever run: true (only a README ever in docs/study/rounds; progress/ empty everywhere; exportJSON() only called from a test; MEASURED). Also confirmed: 5e9c981 says the ch04–05 review "is owed", and index.html:112 still says only chapter 1 is ready.

## Findings and disposition

The dispositions are the coordinator's, read off its synthesis in `../plan.md` (Outcome, parts 1–5 and 4(a)–(g), cited here as "synthesis 5" and so on). "Not taken up" means the synthesis does not mention the finding; it is not a rejection. The recorder added no judgement of its own. The first five rows are this round's own findings; the rest are its checks of findings from rounds 0–3, which keep the IDs those rounds gave them.

| ID | Finding | Disposition and reason | Repair or follow-up |
|---|---|---|---|
| F48 | Recommendation: budget each chapter at about 40M weighted tokens, half of today's ~70M. Pay for it with cuts no reader sees — compact near 250k, a fresh coordinator each round, `AGENTS.md` cut to its rules with the Gates section moved to a looked-up doc, no big-context agent idle past the 5-minute cache — and with a cap of at most four bespoke interactive figures a chapter, authored static illustrations elsewhere. Spend about 5% of the saving on an accuracy review of each chapter against the matching OpenStax Biology 2e section, and on a study loop that can run. Chosen from seven costed alternatives, (a)–(g). | Adopted by the coordinator as its advice; synthesis 5. The synthesis also includes running one textbook session at a time, which this report does not propose, and states the verification change from alternative (e) as part of the advice. | The parts that change rules or harness settings are pending with the owner (listed at the top of `../plan.md`). The one-week test in synthesis 5 is how the change would be judged. |
| F49 | The bottleneck is the weekly usage allowance: weekly-limit messages start two silences of 114 h and 134 h, and the transcripts have records in only 40 of the 351 clock-hours since the first one. A chapter costs about 70M at the margin, and the textbook's share of the account is roughly 190–280M a week (INFERRED). | Accepted; synthesis 1. | Whether to buy more usage is pending with the owner. |
| F50 | The current way of working is not aimed at the bottleneck: nothing counts tokens per chapter; the most expensive agent (gate-speed, 21.8M) made gates faster while gate time is not scarce; `AGENTS.md` doubled in two weeks and loads into every agent; extrapolated to 32 chapters, a full gate run takes about 2.8 h, and it must pass before every code commit. | Accepted; synthesis 5 carries the 2.8 h. | As F48. |
| F51 | What the recommendation gives up: about half the bespoke figures and figure tasks per chapter (tasks are 27% of items), full gate-chain runs on ordinary commits, and the recipe's "six to nine figures, no static diagram" rules. Overruling those rules is the owner's decision. | Accepted as stated; synthesis 5. | The figure cap, and the recipe rule it overrides, are pending with the owner. |
| F52 | Other levers: all 32,254 model-tagged records name Opus 5, Opus 5.5 or Fable 5.1, and a smaller model for gate-running, waiting and landing helps only if the plan meters it cheaper; park the Chinese edition; a larger plan lifts the constraint directly. | Parking the Chinese edition and buying more usage are decisions pending with the owner. The smaller-model point is not taken up. | Pending with the owner. |
| F5 | Check 1: correct-option-first is true for the 233 review-bank questions Today serves (`mastery.js:799–805`, no shuffle); "every multiple-choice answer on main is A" overstates, since the 25 in-chapter questions put the answer at B (18) or C (7). | Accepted; the correction is applied in synthesis 4(b). | The fix is on `choice-order`, not merged. |
| F24 | Check 2: 46 runs, 3 green and 43 red since 09-11. "All 43 failed on the chapter-3 negative rect" is wrong — `0482dde` shows ~3,000 negative-size errors, but `6d854cf` failed the content check; "39 of the last 40" holds. | Accepted; the correction is applied in synthesis 4(c). | None in this round. |
| F17 | Check 3: the lockout shape holds (limit messages at 09-11 07:49Z and 09-17 13:05Z), but "coverage complete" and "nothing ran inside them" are wrong: the main checkout's reflog has 19 commits between 10:11 and 20:19 PDT on 09-17, after that day's limit hit, that no Claude or Codex transcript on this machine covers. The lockout share is nearer 71%. | Accepted; applied in synthesis 1 (71–75%). | None. |
| F10 | Check 3, continued: for the same reason, the 599.7M total is a floor. | Accepted; applied in synthesis 3 ("at least 600M … a floor"). | None. |
| F1 | Check 4: no figure since 09-17 is true (the newest is atp3d, 09-17 06:04), but about 5 of those 6 days were lockout, so it is a stall of about one working day. | Accepted; applied in the synthesis's list of corrections. | None. |
| F4 | Check 5: no study round has ever run — only a README has ever been in `docs/study/rounds`, and `progress/` is empty everywhere. | Confirmed. | As F4 in round 0. |
| F29 | Check 5, continued: `exportJSON()` is called only from a test. | Confirmed. | As F29 in round 3. |
| F7 | Also confirmed: `5e9c981`'s commit message says the chapter 4–5 review "is owed". | Confirmed. | As F7 in round 0. |
| F6 | Also confirmed: `index.html:112` still says only chapter 1 is ready. | Confirmed. | As F6 in round 0. |

The report's statement of the goal, and the token and week estimates for each alternative, are recorded as reported and carry no finding ID.

## Verification

The critic's checks were inline node, grep and gh runs against the repository, the reflog, the transcripts and the Actions record, each tagged in its report; it kept no files. Nobody re-checked its estimates of tokens per chapter or weeks remaining, which it marks INFERRED. The recorder re-ran nothing.

## Round outcome

The coordinator adopted the recommendation as its advice (synthesis 5) and applied the critic's four corrections — to F5, to the reading of F24, to F17 and F10 together, and to F1 — before writing the synthesis; check 5 and the two further confirmations needed no change. What remains is the owner's: the six decisions at the top of `../plan.md`. The one-week test in synthesis 5 is the measure of whether the change works.
