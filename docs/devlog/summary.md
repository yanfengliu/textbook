# Devlog Summary

- 2026-09-23 — **Chapter 6, Photosynthesis, is published, and the round stops there by the owner's decision.** Four figures and 120 items shipped, with two independent reviews applied: the prose review, and the OpenStax accuracy review with its ten figure findings. The chapter is joined to the contents page, chapter 5's card and Today. It landed from `land-3` together with `ch04-figs`, `site-and-ci` and `term-markup`. Two defects only the combined tree had: the glossary's fixed term column was 13 px too narrow for "Photophosphorylation", and a true hyphen in Figure 6.1 fell outside chapter 6's font subset and was drawn by Segoe UI. The figure workers' red proofs were re-run on the landing tree rather than copied from their handoffs. `tools/drive.js`'s count of polls and sleeps turned out not to be reproducible from any commit, and now states its method. No independent review of the integration edits was done beyond the accuracy review. Chapters 7 and 8 stay on `origin/ch06-07`, `origin/items-ch07` and `origin/ch08-dna`. Detail: `detailed/2026-09-23-chapter-6-landing.md`.
- 2026-09-23 — **A glossary term keeps its sub- and superscripts, and its card no longer runs off a phone's screen.** `<tb-term>` set its label from `textContent`, so chapter 5's NAD⁺ read "NAD+", and chapter 6's NADP⁺, C₃ plant, C₄ plant and cytochrome *b*₆*f* read flat. The label now keeps `sub`, `sup`, `i`, `em`, `b` and `strong`, and across the 96 terms of the two chapters Chrome's accessible names, the Tab order and the cards did not change. On a phone, 33 of chapter 5's 46 cards and 32 of chapter 6's 50 ran past the right edge and let the page scroll sideways, NAD⁺'s at 63–415 px on a 390 px screen; so did 256 of 360 sampled on the second book. `place()` put a card at `left: 0` to measure it and clamped it against `innerWidth`. On a mobile browser a box past the right edge grows the layout viewport, which is what `innerWidth` reports, so the clamp read a width the card had just inflated; a 390 px desktop window never showed it. The card is now parked off the page's start edge while it is measured, and clamped to `visualViewport.width`. Under mobile emulation all 192 openings on chapters 5 and 6, in both themes, land inside 8–382 px with no sideways scroll. Desktop positions are unchanged at 1440 and 390 px, except that with a space-taking scrollbar a narrow window's cards now stop 8 px short of the scrollbar instead of running 7 px under it. `npm run devices` passed the defect: it measures a card against `innerWidth`, and looks for sideways scroll before any card is open. That bound is open, and no gate was added, by the owner's direction.
- 2026-09-23 — **The library page and README said only chapter 1 existed, with five published; the class is gated, and CI is sized to finish.** Both texts now point at the book's contents page instead of naming chapters. `checkChapterAvailability` in `npm run check` holds each book's contents page to its chapter directories, and reads the shelf cards and README.md for lists and counts of ready chapters. It is red on the old texts and on `6d854cf`'s contents, where this class started, and an independent review found four holes in it before it landed, including a misread of the very sentence its own failure message recommended; all four are closed. `ci.yml` installs WebKit and Firefox beside chromium, is pinned to `ubuntu-24.04`, and allows 240 minutes where 30 could not be met (unit, check, shot, flow and drive alone used about 15). A branch run on GitHub (`35938466158`, through `workflow_dispatch`) proved the install and the runner (4 CPUs, 15 GB), then stopped at `drive`'s two Linux-only `bilayer` steps, so `devices` and the timeout are still unexercised. `docs/design/textbook.md` still says only chapter 1 exists. Detail: `detailed/2026-09-23-the-site-said-one-chapter.md`.
- 2026-09-23 — **Chapter 4's figures: `bilayer` said "Micelles" at 90 °C because its verdict was one sweep of a thermal walk, and its tank's physics depended on the reader's screen.** The verdict is now what the tank held over the last second of its clock, with hysteresis — at 90 °C, 0 of 23 runs' reads anything but dispersed, where one sweep had been a sheet or micelles in 8.8-12.8% of reads — and the tank's height floor went from 0.52 to 0.66 of its width, because a 1024 px page's 10.4 nm tank held a sheet or micelles in 89% of sweeps at 90 °C. The words after Curl say what Curl did. Two drive steps walk the clock with Step instead of reading it at an instant the machine chose, and a Node test holds the flattest tank. The osmometer's words agree with the rewritten §4.4, and chapter 4 is off `npm run legible`'s deferral list, its 30 pairs under AA fixed. Detail: `docs/devlog/detailed/2026-09-23-chapter-4-figures.md`.
- 2026-09-23 — **The owner accepted the project audit's recommendations, and they are now written where every agent reads them.** The usage allowance is the budget, about 40M weighted tokens a chapter against ~70M measured; contexts set to compact near 250k (lands separately; not yet observed); at most about four bespoke interactive figures a chapter, with authored static SVG allowed; an accuracy review before a chapter ships; the study loop first when study work resumes; `npm test` stays the commit gate until a core gate exists; one textbook session at a time; the Chinese edition parked; and a stop after chapter 6. `AGENTS.md`'s Gates body moved word for word to `docs/policies/gates.md`, taking `AGENTS.md` from 58,591 to 36,493 bytes. Direction: `docs/policies/local-rules.md`; evidence: [7_project-audit](../work/7_project-audit/plan.md).
- 2026-09-23 — **Chapters 4 and 5's review fixes landed, with the drawn option order, the sort layout and the ATP ladder, on top of the theme-flash landing.** The 2026-09-22 prose review of both chapters is applied and committed under `docs/work/2_rest-of-the-book/reviews/`. The correct option was the longest in 42 of 43 of chapter 4's multiple-choice items and in 44 of 44 of chapter 5's; rewording the options, never reordering them, brings both to 11. Chapter 5 now does its arithmetic with RT = 2.58, so every chain it prints checks. `npm run check` now fails a glossary term with markup in it, which five terms in the two chapters had printed as text. `26d47cf` reached main while the five branches were being merged, so the landing merged it too: eleven conflicted files across four merges, all shared records apart from one log line in `tools/shot.js`, each resolved by keeping both sides. No independent review of the landing has been done.
- 2026-09-23 — **Figure 5.3's ladder was already the chapter's way up; what it got wrong was around the rail.** A brief said the rail ran upside down against §5.3's table. Read off the drawn SVG and photographed at five widths in both themes, and on the live chapter page, phosphoenolpyruvate −61.9 is at the top and glucose 6-phosphate −13.8 at the bottom. What was wrong was around the rail. The verdict put the phosphate onto the product: "Phosphoenolpyruvate can phosphorylate ATP" now reads "can phosphorylate ADP and make ATP". A "cannot", in the verdict and in the rule under it, now says "under standard conditions". ATP phosphorylating creatine is how a resting muscle recharges its reserve, so the flat "ATP cannot phosphorylate creatine" was false about cells. The Donor and Target ranges counted rungs from the top, so ArrowUp, and a button announced as "Donor, step up", moved the marker one rung down. The ranges now hold a height on the rail, and the buttons are ↑ and ↓. A step button's width is now the bench's, 1.6rem whatever its glyph, because the wider arrows had pushed a 320 px toolbar onto an extra row. The rail and its table now name the quantity as §5.3's table does on `fix-ch05`, "ΔG°′ of hydrolysis", where they said "RELEASED ON HYDROLYSIS" over negative numbers; the two branches must land together. A verdict that wrapped to three lines lost its last line, and is now drawn whole. The brief's second defect, a measurement taken before the webfont arrives, was already answered by the bench's `document.fonts.ready` redraw. With the fonts held back, the words moved from 248.1 to 250.3 and from 333.1 to 342.2 on arrival, and the pane then matched a fonts-first run byte for byte. `npm run drive` now reads the drawn ladder in four steps, and nine arms went red. Both CLI reviewers were down, so a read-only subagent reviewed it twice; its findings are fixed in `c5777c1` and `790dbae`. Detail: [2026-09-23-the-ladder-was-already-the-right-way-up](detailed/2026-09-23-the-ladder-was-already-the-right-way-up.md).
- 2026-09-23 — **A sort's names collapsed to nothing at desktop widths, and no gate measured a text box against its own words.** `<tb-sort>` gave its choices' whole row to a grid track first, so chapter 5's names were 0 px wide at 1024 and 1440 px, chapter 4's 18.8 and 60.4 px, chapter 3's a 148–190 px ribbon, and chapter 2's 10.8 px in an 800 px window. A row is now a wrapping flex line whose choices go under the name when the two do not fit, on every row of a sort at once (`watchRows` in `sort.js`), and a wrapped name no longer ends on a one-word line; below 600 px every sort keeps its exact size. `npm run shot` now fails a reader component's text box narrower than its longest word, a sort name wrapping in less than 15 em, and a sort whose rows split, proved red on the shipped grid. Found and left open: the second book's character card runs its unbound-sense note out of the card at 390 px, and five glossary terms in chapters 4 and 5 print their `<sub>` or `<sup>` tags as text. Detail: [2026-09-23-the-sort-and-its-words](detailed/2026-09-23-the-sort-and-its-words.md).
- 2026-09-23 — **The chapter checks are shuffled too, and an explanation that names an option by its place fails the content check.** A check's order is drawn from its id and its page, and over the 36 checks the correct option now sits at A 5, B 13, C 8, D 10 (chi-square 3.78, against **32.00** as written). `npm run check` fails "the last option", "option B" or 「前两个选项」 in a check or a bank, and 通鑑 chapter 3's q-bianjie now names its options by what they say. The six such explanations in chapters 4 and 5 belong to the workers rewording those pages, so this branch is red on `npm run check` until theirs land; all fifteen in chapters 6 to 8, on branches still in flight, will fail it too. Two beliefs died: that `tools/flow.js` already pressed check options by `data-correct` (its biology steps pressed by position, and under the draw the keyboard step fails), and that the chi-square bound would catch a seed without the page (it scores 4.22; a two-page claim holds it). `npm run flow` also cannot see a check that marks the wrong option correct, and its frames of an answered check show no tint, because they are taken mid-transition. Detail: [2026-09-23-the-drawn-option-order](detailed/2026-09-23-the-drawn-option-order.md).
- 2026-09-22 — **Every multiple-choice question in Today could be answered by pressing A.** All five item banks were written with the correct option first (49/49, 52/52, 45/45, 43/43, 44/44) and the sitting showed them in that order from `22553d8` on, so a reader who knew nothing was recorded as right. Today's options now appear in an order drawn from the item's id and the number of answers to it the record already holds (`src/components/choice-order.js`); the letters follow the screen, and `chose` still names the bank's letter, so records and `npm run review` are unchanged. Gated by `test/choice-order.test.js` — a chi-square bound over every bank, driven through the component's own paths — and by a read-back in `npm run sitting` whose first version could not fail on half its presses. The chapter checks were shuffled on this branch and then put back: their correct option sits at B or C in 34 of 36, but seven of their explanations say "the first option" or "the last option", so a drawn order would point those sentences at the wrong options; they need rewording first. Two things a later reader could trip on: Today's first calibration question draws its correct option at A on a first asking (a 1-in-4 draw, not the defect), and the correct option is still the longest in 220 of 233 items, so picking the longest scores 93% — a content job no order can fix.
- 2026-09-22 — **`npm run shot`'s frames are not reproducible, and the largest reason was a theme flash a reader could see.** `src/shell.js` applied the reader's stored theme after the page had been styled once, so a reader whose choice differed from their system saw the whole page in the other theme for one to three frames and then its titles fade across in a 240 ms colour transition — Today on 6 loads of 6, the biology contents page 5 of 6, chapter 1 4 of 6, the library 2 of 6 — and the gate photographed the fade. Fixed by `THEME_BLOCK` (`src/theme-early.js`), a classic script in the `<head>` of all 13 pages that load the shell, held by `test/theme-early.test.js` and by `npm run theme`, **a new step of `npm test`, which now has thirteen**; four mutations turn the text check red, and the browser check red on three, the fourth — the block moved to the end of `<head>` — being its recorded bound. The shutter now waits for every running animation, 120 frames at most. Three full runs of one still tree, over the 12 pages it shares with the 2026-09-17 tree: **61 of 72 frames byte-identical, 8 differing per pair**, against 39 of 72 and 27, 24, 27 on that tree; the 11 left are SVG label advances, `backdrop-filter` blur, a stage's rounded corners and one unexplained 21-pixel speck, all named in `tools/shot.js`'s header, and `AGENTS.md` no longer says a screenshot is the same frame every run. Four beliefs died: the library's difference was not 1/255 rounding (up to 27/255, a colour transition), not the header's blur (the heading), the SVG class is a font-cache collision and not a font-arrival race, and Today was never stateful. Open, for the owner: fix G or U for the SVG class, and the blur and corner residue; and `soup`'s world depends on when the fonts arrive, recorded in the defect register with no gate yet. An independent review found no blockers and seven findings, all fixed. Detail: [2026-09-22-the-theme-flash-and-the-shutter](detailed/2026-09-22-the-theme-flash-and-the-shutter.md).
- 2026-09-22 — **Chapters 4 and 5 reached main in `5e9c981`, with the week's gate work behind them.** Chapter 4, Membranes and transport, and chapter 5, Energy and metabolism, landed with eight new figure modules each, 111 and 117 review items, and 21 and 33 prerequisite edges back into earlier chapters; chapter 5's figures are built on a shared bench, `src/figures/lib/bench.js`. `npm run pinned` and `npm run legible` became chain steps, `npm run narrow` began pressing each toolbar toggle, the handshake began waiting for every `<tb-sitting>`, and `npm run devices` went from 1661.5 s to 379.1 s by loading one device per layout shape. The local chain ran 12 of 12 green in 2322 s; GitHub's `test` job went red at `drive`, `bilayer hot-enough-and-nothing-assembles: at 90 °C the jostling should win: micelle at t=8.033, 0.641 buried`. Fixed on published chapters: chapter 3's negative rect width, chapter 2's element symbols under AA in the light theme, and six figures that kept moving with the clock pinned. Left out: chapters 6 and 7, whose sixteen figure kinds did not exist yet (branch `ch06-07`); and `npm run legible` still deferred chapter 4's eight figures. Found after it, and fixed by the 2026-09-23 landing: five glossary terms printing their tags, sort names 0 px wide, and the correct option first in every item bank and the longest in most. No independent review of `5e9c981` has been done.
- 2026-09-17 — **The provenance mark was measured on the rendered page, and every place it was invisible was fixed.** An independent typographic review found five defects the markup check could not see: five `<tb-check>` options received no mark at all, because the rule whitelisted containers and `src/components/check.js` rebuilds each option as a button; every quotation inside a margin note or a `.explain` had an ink step of exactly 1.00:1, which is 17 of the book's 42 marked runs; the 13 px floor inverted the mark on chapter 3's citation line, 13 px against an 11.52 px line, and rested on a measurement that does not reproduce; and a `data-note="textual"` run was repainted out of the note's own 青. All are fixed, and looked at at 1x/2x/3x in both themes. `tools/flow.js` gained a rendered `mark-audit` — 49 runs on chapter 3, 0 not visibly marked, proved red by making the page say marked while showing no mark — and second-book coverage, 37 steps across four pages. The class the owner's question came in through, a 通鑑 run the book prints bare, is now checked: the opener dek, a `q-chici` explanation, a `q-yuren` option, its explanation and all seven sort cards were quoting 通鑑 unmarked in Simplified and now carry the mark. **Two red proofs are owed** — the bare-run check and the figure citation — and are recorded as owed rather than claimed. A devlog detail records what the round proved false and the three landing defects that cost it files. Detail: [2026-09-17-the-mark-and-how-it-was-landed](detailed/2026-09-17-the-mark-and-how-it-was-landed.md).
- 2026-09-17 — **The gate chain could not run in a git worktree, and two standing records' indexes could drift without a word.** `npm run unit` failed in a fresh linked worktree (157 tests / 156 pass / 1 fail, `.git/ is on disk and holds no file`) because `test/control-chars.test.js` required every ignored name to be a directory holding files, and a worktree's `.git` is one `gitdir:` line — so the fleet's own default was impossible here and every session worked in the shared tree, where one session's uncommitted edits mask another's results. The check now reads each named path's shape with `statSync`, keeps the holds-files rule for `node_modules/` and `out/`, and refuses any third shape; proved red on an empty ignored directory. A new gate, `test/standing-docs.test.js`, holds both standing files' indexes to their entries in both directions — on its first run it found a three-commit-old defect (gate-proofs' `legible` entry had no index line, born in the commit that built the grouped index), and its own first proof attempt was a **silent no-op** from the same `[...]` character-class trap that dropped this round's original line. The mark itself: an independent typographic review measured the rendered page and found the container whitelist made it miss five `<tb-check>` options entirely, the 13 px floor inverted it on the citation line, and every quotation inside a margin note or a `.explain` had no ink step at all — 17 of the book's 42 marked runs. All four are fixed, and the fixes were looked at at 3x in both themes. Proof: [gate-proofs.md](../learning/gate-proofs.md); plan and review: [6_mark-quality](../work/6_mark-quality/plan.md).
- 2026-09-17 — **The second book could not say which of its words were 通鑑's, and its own 背景 contradicted its figure.** `lang="zh-Hant"` already marked every 原文 block and every quotation, and `test/lexicon.test.js` already proved each marked run was a corpus quotation — but an attribute is invisible, and the mark was only where an author had remembered it: **21 runs of 通鑑's own words were printed in 背景, 思考 and the 字词 lead as the book's own sentences** (「臣光曰」「以人事知之」「城不浸者三版」「三家分智氏之田」「智伯之臣」「才德兼亡」「保障」「繭絲」), and chapter 2's 背景 said of the 前376 entry 「只转述，不引原文」 four lines above figure 2.1, which quotes it with its citation. Fixed by spending the token the design of record had declared for exactly this and nothing had used (`--zj-quoted-size` in `--ink-soft`, with the 原文 excluded and left at full size and full ink), by marking the 21 runs, by rewriting that sentence, and by a new gate, `test/provenance.test.js`: every 「…」 run of a page's prose that is verbatim 通鑑 must be marked, each `src/figures/zj-*.js` must declare its `QUOTED_FIELDS` in both directions, and the contents page must carry the key to the mark. Proved red six ways — **and arm C1 stayed green on the mutation it was written for** (the key check matched `zj-key-removed`, because `-` is a word boundary), so the check was rewritten and the green arm recorded rather than dropped. `npm run shot` clean on 24 tongjian loads, `drive` and `narrow` green on the three figures; `legible` compares 0 runs for them, which is its own `DEFERRED` list and not a result. Defect: [defect-register.md](../learning/defect-register.md); proof: [gate-proofs.md](../learning/gate-proofs.md).
- 2026-09-17 — **The study page could be photographed before it existed, and three gates were doing it.** `window.__textbook.state` became `ready` on the shell plus, under `?eager=1`, the figures, and said **nothing** about `<tb-sitting>`, whose boot imports five chapters' `objectives.js` and `items.js` and then fetches each `index.html`. So the handshake and that boot were a race. Measured on a still tree, three runs of one command with nothing between them: `SUBPATH_PAGES=today` wrote `today-desktop-dark.png` at **102,349, 163,667 and 50,734 bytes**, and `DEVICE_PAGES=today` wrote its tablet-landscape frame at **109,658 once and 193,829 twice** — the short ones carrying the heading and the dek and **none** of the study surface, and every one of them reported `ok`. The first load of each run was the only reproducible one, because it fetched the fonts over the real network and gave the boot time; every load after it came from the run's own memory and beat it. **The gate's cache was deciding what the page looked like.** Fixed at the root, in `src/shell.js`: `ready` now also requires every `<tb-sitting>` to have booted, so all three gates inherit it and none of them had to know. It is an added conjunct, not a changed one — the figure clause is byte-for-byte what it was, and a held figure still times out with a message that hashes identical on both shells — and it is deliberately **not** behind `?eager=1`, because `devices` does not pass it. A sitting that fails releases the handshake rather than hanging it: `fail()` fills `.tb-standing` just as `paintStanding()` does. Red both ways: with `items.js` held forever, `shot`, `devices` and `subpath` all go red at their 120 s budget, and with the four-line term deleted the same mutation leaves `devices` **green in 843 ms** and `subpath` **green in 2.7 s**, filing stub frames. `tools/shot.js`'s own 600-frame wait was **removed** — a second implementation of `booted` is the thing that drifts — and what it keeps is a census that goes red if the term is ever deleted. Three runs of each gate now byte-identical; `SUBPATH_PAGES` added so a one-page comparison is possible in a tree several people are editing. Also recorded, from another worker's handoff: the scale figure dropped a press that arrived during its glide, 17 reds in 20 wearing the disguise of a flaky gate step ([defect-register.md](../learning/defect-register.md)). Proof: [gate-proofs.md](../learning/gate-proofs.md), the `src/shell.js` entry at the end.

- 2026-09-17 — **The gate chain was 68 minutes and the waiting was not where anyone was looking.** 4076.2 s → 2484.6 s on a frozen copy of the tree, each gate its own process, only `tools/` moving between the arms. `devices` 1661.5 → **379.1 s**: its fixed sleeps were worth 6% (193.7 → 181.5 s on a trimmed run) and pinning the figure clocks was worth **6.6×** (181.5 → 27.3 s), because a chapter mounts nine animated figures and every Playwright action waits for the element to hold still across frames, so all ~1,400 glossary presses queued behind a page that would not stop moving. That is a stated cut: one load per run stays unpinned and the summary prints `running: 1 of 62 …`, so animation-induced layout shift is watched at one device shape, not nine. It was also **red before and green now**, and both failures were `page.screenshot: Timeout 45000ms exceeded` — the gate failing because it was slow. `drive` 542.9 → **327.3 s**: 263.9 s of `waitForTimeout` became 165 polls, 591 assertions and 216 steps unchanged on both sides, 48.9 s of sleep left and every second of it named — the elapsed time is the measurement, or it is settling a frame a person looks at. The conversion produced four regressions and they are the round's real output, now all in `docs/policies/local-rules.md`: the sleep was doing a second job nobody wrote down (`levels`' slider); a poll leaves at the threshold a sleep overshot (`gradient-battery`, 1.202 against 1.30); polling for "stopped" without requiring "started" is a coin toss (six false drawer failures, each of which read as a product defect); and a figure genuinely in motion cannot be caught still (`cilium`'s readout against its own clock). The external-request cache (`tools/lib/net-cache.js`) is worth **15.9 s** on `shot` measured with `NET_CACHE` as the only variable — not the 49.9 s the before/after row shows, because the baseline's `shot` was the first browser gate of a cold session: **the tree held still and the environment did not**. Six red proofs, every mutated file restored to the same hash. `npm run legible` is untouched and is now **58% of the chain on its own**, 1114 s of it six figure-theme pairs at ~185 s, reproduced across two runs and handed to its author. Detail: [2026-09-17-the-gates-were-waiting](detailed/2026-09-17-the-gates-were-waiting.md).

- 2026-09-16 — **A figure's type had never been measured against what is behind it, and ten pairs across three published chapters were under AA.** The palette has two kinds of colour that invert against each other: `ORGANELLES` and the rest are fixed hexes, `ink` and `paper` move with the theme, and the five accents move the WRONG way — `--coral` is a mid red on a light page and a light salmon on a dark one, so ink over it is **1.95:1 dark** and paper over it **3.32:1 light**, and neither reads in both. Not one instance came from a fixed fill; every one came from a colour that moves — a figure accent spent as 10 px type (3.07–4.36:1 in the LIGHT theme, in `tree`, `scale`, `homeostasis`, `bondlab`, `polymer` and `foldlab`), or ink and paper written over an accent fill (`levels`'s frog heart at **1.49:1**, its bond angle at **1.09:1**, `bondlab`'s δ+ at **1.00:1** on its own oxygen). All fixed with the book's own `--*-text` accents, a fill mixed further towards the paper, or the halo `scale` and `tree` already use; no new colour. Two non-colour defects came out of it: nine `levels` labels asked for a colour with an SVG `fill` ATTRIBUTE that `.tb-levels svg text` beat every time, and `symbiont` threw `Cannot access 'tw' before initialization` on every pinned-clock load. Gate: **`npm run legible`**, step eight of `npm test`, every figure in both themes at 1000x640, driven through its own controls, three frames per state — the magenta one is the glyph mask, because a mask read off the figure's own frame would report the defect as "this text draws nothing". **4 min 20 s, 9,631 glyph runs, 50 figure/theme pairs, green**; it was 24 minutes until the control loop stopped addressing a rebuilt toolbar by index. Chapter 4's eight figures (13 pairs) and the second book's three (35) are in `DEFERRED`, measured and printed on every run. The one element table's `label: 'paper'` — 2.26:1 on a gold disc in the light theme — is four floored `ALLOWED` entries and a written-up decision for the integration owner. Detail: [2026-09-16-gates-that-could-not-say](detailed/2026-09-16-gates-that-could-not-say.md).

- 2026-09-19 — **A worktree is the default, and one left on disk after its work has landed is a defect.** The fleet canon's Coordinate-safely bullet made isolation in a worktree the default rather than one option of two, made finishing mean merging to main and pushing in the same session, and made a surviving tree a defect to report; canon `bb31c741ed27` → `95bcbcb491dd` across 27 repos, with **25 carrying it on HEAD and origin**. `tiny_people` and `voxel` could not land, because another session holds uncommitted prose in the same `AGENTS.md` outside the block, so their working trees carry the new stamp while a clean checkout still reads the old one — nothing stashed, nothing forced. `docs/policies/local-rules.md` was rewritten to stop ratifying serialised writes and to put the one real constraint where it belongs, the work-doc ID allocation rather than the working tree, since `../fleet/scripts/work-docs.mjs` resolves the primary checkout through Git's common directory. Three corrections landed with it: the allocator's refusal is not one misnamed review file but that the tree predates the schema — the name pattern and a six-section template are enforced together and **0 of the 9** review files carries the first two sections — `Measure-Object -Line` counts non-blank lines and nearly turned a true count into a false one, and a canon candidate was staged in `../fleet` for the case that produced it: a number read off a downscaled image preview is not a measurement. Detail: [2026-09-16-gates-that-could-not-say](detailed/2026-09-16-gates-that-could-not-say.md).

- 2026-09-16 — Rare characters in the 通鑑 原文 were drawn by the reader's own machine: the pages' Google Fonts request carries no `text=` parameter, so the two families arrive as `unicode-range` slices and **21 of the 1,937 code points the book uses fall outside every one of them** — 蜹、蠆、藺、鼃、驂、讒 on chapter 2 and fifteen more — which reads as almost right on a machine that has SimSun and as tofu on one that does not, in a book whose text is the point. It was a slicing gap and not a font gap, and the page had loaded the right two families, which is why nobody noticed: **a page can load a face and still not cover a character.** Every `tongjian/` page now loads the same two families a second time with a `text=` list of 18 — the six that failed, 紂 U+7D02, which stands only on a card the census cannot open, and eleven witness forms a card prints — costing **13,236 B** on a page that draws one of them and **0 B** on a page that draws none; 𠆸 U+201B8, 𥳑 U+25CD1 and 𣳘 U+23CD8 are excluded deliberately, since nine families were tested and none draws CJK Extension B. Gate: `npm run shot`'s font census, the check that found it — the four-page run ended `shot: 24 page loads clean`, exit 0, and chapter 2 now reads `1644 glyph(s) over 1644 code point(s) in 14 stack(s): Noto Serif SC ExtraLight:1438 Noto Sans SC Thin:206`.

- 2026-09-19 — The second book read by its owner and by two reader-proxies, and ten defects came out of it, six of them things no gate was asking about. The owner's two: the header's contents button appeared to do nothing on a phone — re-measured, it works on the working tree and on the published site at 320/360/390/430 px on both books, and the record's explanation is the **19 px** target the header squeezed the icon buttons to, which `4c4acb8` fixed; and the reading column was a fixed 698 px, so it took 36% of a 1920 px screen and 27% of a 2560 px one, now a ladder of `@media (min-width: …)` rungs in `tongjian/zj.css` giving 66% at 1280, 58% at 1440 and 53% at 1920, with the 原文's grid stepping 17 → 24 characters. From the reader-proxies: 1,291 character cards opening on hover over the paragraph being read, a card that never closed (the first fix listened for `scroll` and the biology book's hover closed on a scroll nobody performed), and a 72-entry 字詞 list whose `localeCompare` order was ICU radical-and-stroke — 三版, 不如, 二心, 人臣 … 飲器, 驂乘, 魏斯 — so it had no order a reader could scan; it now carries an index of its first characters. **The one worth reading is a cosmetic property hiding a click**: `tb-term` was `display: inline`, so appending the card into it gave the CJK line breaker a break opportunity, the justified 原文 re-broke, the term moved 318 px and the click was dispatched on an ancestor — fixed with `display: inline-block`, 0 of 1,625 glyphs moved. An independent review also caught 夫 in 「夫才與德異」 shown as `fū`, which needed a `reading` field on a use: 42 uses across 15 entries, 13 chapter bindings, and the first gates here that check what a card **shows** rather than what the file declares. Commits `e6daf7a` and `0482dde`; the width, the colour round and two new device checks — the left header corner, and a 50% width floor for this book at 1280–1920 px — are uncommitted in the working tree; the two device checks have since been proved red, three mutations in `docs/learning/gate-proofs.md`, and the proofs still owed are the `圖`/`图` caption check's and the two 多音字-reading checks'. Detail: [2026-09-12-tongjian-and-the-crash-dialog](detailed/2026-09-12-tongjian-and-the-crash-dialog.md).

- 2026-09-18 — Second book published: 资治通鉴 卷一 周纪一 in three chapters, the 原文 beside a modern translation with every one of its 1,405 characters clickable (640 lexicon entries, 1,171 quotations verbatim inside a tracked corpus of 13 entries transcribed from four or more fetched witnesses each). Two independent reviews found what no gate could see: a 多音字 showing the wrong reading, two figures clipping at 390 px, a selected chip at 1.22:1, one term in 172 opening nothing on a phone. Four owner revisions landed: 原文 holds only the text, 原文 and quotations Traditional with everything else Simplified, 怎麼讀這一章 deleted, chapter navigation added. Commit `e6daf7a`, live at <https://yanfengliu.github.io/textbook/tongjian/>. Detail: [2026-09-12-tongjian-and-the-crash-dialog](detailed/2026-09-12-tongjian-and-the-crash-dialog.md).

- 2026-09-12 — Second book started: 《資治通鑑》 周紀一 as 原文 beside 現代漢語翻譯, with a clickable 字/詞 lexicon whose every quotation is a verbatim substring of a tracked corpus. Chromium's crash dialog diagnosed and silenced at launch (`tools/zj-quiet-browser.cjs`, gated by `test/browser-quiet.test.js`), and two environment facts recorded in `docs/policies/local-rules.md`: the test runner needs `--experimental-test-isolation=none` here, and the two pre-existing `npm run check` failures belong to chapters 2 and 3. Detail: [2026-09-12-tongjian-and-the-crash-dialog](detailed/2026-09-12-tongjian-and-the-crash-dialog.md).

- 2026-09-17 — A gate failing one run in five was traced to a poll rounding its own reading: a drawer's ease spends its last 90 ms travelling 2 px, so 5.5 % of polls stopped one rounded pixel short of rest, and that short reading disarmed the next wait's guard. Found by sampling the mechanism 400 times when the gate itself would not fail in twenty runs. The legibility gate's surface rule became an order statistic, which took it from 1 finding to 23 by seeing figures drawn in gradients for the first time. `npm run pinned` shards 8 ways, 7m28s to 1m21s, with the honesty rule untouched.
- 2026-09-17 — Nine figures were advancing their own clocks while pinned, breaking the invariant every page gate trusts; two fixed, six named, and `npm run pinned` built to catch the class. Its first version went **green with the defect in the file**: pressing controls in DOM order finished the animation before it reached Play. Chapters 4 and 5 written, with 21 and 33 prerequisite edges reaching into earlier chapters where chapter 3 originally had none. The gate chain cut from 4076 s to about 2480 s, the largest single saving being that a page whose figures are running makes every simulated tap wait for the animation to settle. Seventeen text-legibility defects fixed across published chapters, and the element colours deepened so a symbol on a disc clears AA in both themes.
- 2026-09-16 — Main was found carrying this round's whole tree, committed by the second book's session the night before with one figure mid-build and no chapter-3 bank. Recovered forwards: chapter 2's eight figures finished at phone width (soup's narrow key band clipped, its narrow shape taller so the protein a question asks for is on screen, waterprops' heat label clear of its line and its freezing preview complete mid-freeze, phlab's tight-pane inset re-gapped); chapter 3's eight figures finished and accepted, the last four having passed every gate on arrival while a look found seven defects in the unfinished one; the constitution's two 2026-09-15 directives carried in. Greek letters and ⇌ set in Libertinus Serif after measurement showed the first choice a tenth too tall; the last system-font glyphs on the biology pages gone; chapter 2's two element tables merged into one with a test.
- 2026-09-15 — Nine workers killed by a rate limit; recovered from the tree, one red-proof mutation found left applied. `tools/inspect.js` empties its directory per run (it was overwrite-only, so a two-selector run left frames 02–04 of an earlier run in place and a redesign was reviewed against a pre-redesign frame); `--label` keeps a second run for before/after; the selector is in the filename. Chapters 2 and 3 corrected from the independent review, with sources. `npm run sitting` now reads its label class and its "learned well" word from the component instead of literals and fails when it finds no label at all; the old gate, run against a translated word, a moved class and a page with no labels, passed all three. Detail: [2026-09-11-gates-that-did-not-run](detailed/2026-09-11-gates-that-did-not-run.md).
- 2026-09-11 — Furniture redesigned to one rule: the figure stage is the only drawn rectangle in the book. Chapter opener composed around a head rule and a folio; `tb-check` and `tb-sort` set with rules and space instead of nested cards. Two contrast failures fixed, one at 1.57:1. Bilingual design of record written and accepted. Detail: [2026-09-11-gates-that-did-not-run](detailed/2026-09-11-gates-that-did-not-run.md).
- 2026-09-11 — Four checks found passing without checking: a figure could overwrite the frame's `state`, a chapter with no review items passed the content check, no page gate visited chapters 2 and 3, and the device gate was not in `npm test`. All four gated; chapters are now discovered from disk. Detail: [2026-09-11-gates-that-did-not-run](detailed/2026-09-11-gates-that-did-not-run.md).
- 2026-09-10 — Adaptive study: mastery per objective with a prerequisite graph, a pure scheduler, a Today page, 99 review items, and agent rounds. Published on GitHub Pages. Typography, contrast and figure polish. Detail: [2026-09-10-adaptive-study-and-pages](detailed/2026-09-10-adaptive-study-and-pages.md).
- 2026-09-09 — Chapter 1 integrated: nine figures from three workers, `drive` gate exercising every figure's controls, `flow` gate for the reader controls, frame fix for figures that report ready inside mount. Detail: [2026-09-08-foundation-and-chapter-1](detailed/2026-09-08-foundation-and-chapter-1.md).
- 2026-09-08 — Repo joined the fleet census; design of record, site foundation (tokens, type, shell, components, figure contract), chapter 1 prose, and the check/shot/unit gates written and proved red. Detail: [2026-09-08-foundation-and-chapter-1](detailed/2026-09-08-foundation-and-chapter-1.md).
